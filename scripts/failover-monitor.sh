#!/bin/bash
# This script will monitor the health of the primary and secondary uplinks.
# It will contain logic to detect a failure and automatically switch
# the default route to the secondary gateway.
